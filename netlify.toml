[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: every route (e.g. /night/ABX82K) must serve index.html
# so React Router can handle it client-side, instead of Netlify 404ing.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
